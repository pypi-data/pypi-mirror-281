def 列表_全员减法计算(列表, 要减的数值):
    """
    将列表中的每个元素都减去指定的数值，并返回结果列表。

    参数:
    - 列表 (list): 包含数字的列表
    - 要减的数值 (int): 要从每个元素减去的  整数或小数 数值

    返回:
    - 新列表 (list): 包含所有元素减去指定数值后的结果

    如果遇到任何异常（例如类型错误），则直接返回空列表。
    """
    新列表 = []

    for 元素 in 列表:
        try:
            新列表.append(元素 - 要减的数值)
        except Exception:
            # 如果出现异常，直接返回空列表
            return []
    return 新列表
