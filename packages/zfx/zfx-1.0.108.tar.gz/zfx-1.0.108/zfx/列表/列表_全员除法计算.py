def 列表_全员除法计算(列表, 要除的数值):
    """
    将列表中的每个数字元素都除以指定的数值，并返回结果列表。

    参数:
    - 列表 (list): 包含数字的列表
    - 要除的数值 (int or float): 要除以每个数字元素的数值，不能为0

    返回:
    - 新列表 (list): 包含所有数字元素除以指定数值后的结果

    如果遇到非数字元素（如字符串）或者要除的数值为0，则直接返回空列表。
    """
    新列表 = []

    for 元素 in 列表:
        if isinstance(元素, (int, float)) and 要除的数值 != 0:
            新列表.append(元素 / 要除的数值)
        else:
            # 如果遇到非数字元素或者要除的数值为0，直接返回空列表
            return []
    return 新列表
