def 列表_筛选_关键词过滤(列表, 关键词):
    """
    过滤掉列表中包含指定关键词的元素，并返回过滤后的新列表。

    参数:
        - 列表 (list): 包含字符串元素的列表。
        - 关键词 (str): 要过滤掉的关键词。

    返回:
        - 过滤后的新列表 (list): 只包含不包含指定关键词的元素的列表。
    """
    try:
        过滤后的新列表 = [元素 for 元素 in 列表 if 关键词 not in 元素]
        return 过滤后的新列表
    except Exception:
        return []