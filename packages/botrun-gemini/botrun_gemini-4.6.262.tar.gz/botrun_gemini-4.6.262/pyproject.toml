[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "botrun_gemini"
version = "4.6.262"
dependencies = [
    "python-dotenv",
    "google-generativeai",
    "google-cloud-aiplatform"
]
readme = "README.md"
scripts = { "botrun-gemini" = "botrun_gemini:main" }
