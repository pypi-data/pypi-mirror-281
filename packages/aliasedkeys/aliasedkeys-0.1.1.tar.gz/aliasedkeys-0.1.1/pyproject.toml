[tool.poetry]
name = "aliasedkeys"
version = "0.1.1"
description = ""
authors = ["Matthew Young <myoung.space.science@gmail.com>"]
readme = "README.md"
packages = [{include = "aliasedkeys", from = "src"}]

[tool.poetry.dependencies]
python = "^3.10"
nonstring = ">=0.1.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
