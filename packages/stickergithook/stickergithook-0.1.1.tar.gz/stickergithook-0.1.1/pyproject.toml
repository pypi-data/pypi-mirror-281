[tool.poetry]
name = "stickergithook"
version = "0.1.1"
description = ""
authors = ["Acelynn Zhang <azyx0987e@gmail.com>"]
readme = "README.md"

[tool.poetry.scripts]
sticker = "stickergithook.main:app"

[tool.poetry.dependencies]
python = "^3.10"
typer = {extras = ["all"], version = "^0.12.3"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
