[tool.poetry]
name = "dvh-tools"
version = "0.5.2"
description = ""
authors = ["NAV", "DVH"]
license = "MIT"
readme = "README.md"
packages = [{include = "dvh_tools"}]
repository = "https://github.com/navikt/dvh_tools"

[tool.poetry.dependencies]
python = "^3.11"
google-cloud-secret-manager = "^2.20.0"
google-cloud-bigquery = "^3.23.1"
oracledb = "^2.2.0"
pandas = "^2.2.2"


[tool.poetry.group.dev.dependencies]
pytest = "^8.2.1"
black = "^24.4.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
