[tool.poetry]
name = "my-app-poetry"
version = "0.0.6"
description = "Descrição do seu pacote"
authors = ["Seu Nome <seu.email@example.com>"]
readme = "README.md"
packages = [{include = "my-app-poetry"}]

[tool.poetry.dependencies]
python = "^3.8"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
