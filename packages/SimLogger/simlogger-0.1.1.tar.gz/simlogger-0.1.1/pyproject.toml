[project]
name = "SimLogger"
version = "0.1.1"
readme = "README.md"
authors = [{ name = "Joey Kilgore", email = "joey.a.kilgore@gmail.com"}]
dependencies = [
    "logging",
    "pickle",
    "datetime",
    "pathlib",
    "pandas",
    "os"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
