{% for pin in pinned_history %}
- name: "pin_{{ loop.index }}"
  raw_string: |
    {{ settings.start_token }}{{ escape_sequences(pin) }}{{ settings.eom }}
  {% set settings.start_token = settings.bom %}
{% endfor %}
