from setuptools import setup, find_packages

setup(
    name='librarylab1',
    version='0.1',
    description='A Python library for loading input values for automatas.',
    author='Dinca Andrei',
    packages=find_packages(),
)