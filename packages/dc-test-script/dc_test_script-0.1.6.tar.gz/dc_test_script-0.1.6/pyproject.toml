[project]
name = "dc_test-script"
version = "0.1.6"
description = "Add your description here"
authors = [
    { name = "DCrawley94", email = "danikacrawley.dev@gmail.com" }
]
dependencies = []
readme = "README.md"
requires-python = ">= 3.8"
license = "MIT"

[project.scripts]
"test-script" = "dc_test_script:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/dc_test_script"]
