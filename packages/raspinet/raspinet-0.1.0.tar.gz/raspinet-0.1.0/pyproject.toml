[tool.poetry]
name = "raspinet"
version = "0.1.0"
description = "A library for simplifying networking between Raspberry Pis"
authors = ["Jason Li <jason.ye.li.7@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.6"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
